name: mobile_exemplemvvm
description: "Exemple pédagogique MVVM en Flutter"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Gestion d'état MVVM
  provider: ^6.1.1
  
  # Pour faire des appels HTTP (simulation API)
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

